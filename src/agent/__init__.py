"""Last Elevator agent package.

Models propose. Code decides. The Strands agent parses text, ranks feasible options, and drafts
messages. Whether a trip is affected, how many minutes an option adds, and whether a station exists
are code paths with tests (see `policy`, `kb`, and `bart` packages once built).
"""

__version__ = "0.1.0"
