# Evidence: every guarantee, and where to look

Each row ties one promise to the code that enforces it, the test that
proves it, the span event that shows it in a trace, the results file that
counts it, and the claim ids `make verify` checks. `make check-docs`
verifies that every test named here exists.

| Guarantee | Enforced by | Proven by (test) | Span event | Results file | Claim ids |
|---|---|---|---|---|---|
| Never assert a station or elevator outside the KB in a tool call | `KBHook.check` (BeforeToolCallEvent, `cancel_tool`) | `test_two_stage_gate_corrects_every_mistake_once`, `test_full_stack_lets_nothing_hostile_through` | `hook.cancel_tool` | `results/red_team.json` | `red_team.stations`, `exhaustive.stations` |
| Never override BART's option order in the draft | `PlanGateHandler.steer_before_tool` (Guide) | `test_two_stage_gate_corrects_every_mistake_once` | `steering.guide` | `results/red_team.json` | `red_team.options` |
| The final plan carries the KB station and elevator, BART's top option, the policy engine's minutes, also when Strands forces the Plan tool after a prose answer | `PlanGateHandler.steer_after_model` and `check_plan` (Guide discards, model retries) | `test_after_model_gate_rejects_invented_minutes_and_foreign_station`, `test_correct_plan_passes_with_one_proceed`, `test_forced_structured_output_is_gated_and_its_failure_is_delivered` | `steering.guide_after_model`, `steering.proceed_after_model` | `results/red_team.json`, `results/red_team_exhaustive.json` | `red_team.minutes`, `exhaustive.options`, `exhaustive.minutes` |
| Never compute minutes or name a foreign code, even in the rider's sentence | `prose_findings` inside `check_plan` | `test_prose_attacks_are_caught_by_the_after_model_gate` | `steering.guide_after_model` | `results/red_team.json` (`prose_minutes`, `prose_station`) | `red_team.prose_minutes`, `red_team.prose_station` |
| The rider never receives a sentence the model wrote alone | `messages.approved_messages` plus the approval check in `check_plan` | `test_approved_sentences_are_composed_from_the_decision_and_barts_text`, `test_gate_discards_own_words_and_injected_text`, `test_approval_fuzz_accepts_only_whitespace_and_trailing_punctuation_variants` | `steering.guide_after_model` | `results/red_team.json` (`injection`) | `red_team.unapproved`, `exhaustive.unapproved` |
| The rider always gets a plan; the model can only delay it | `deliver` and `DecisionRun` fallback (`composed_plan` at the per-run cap) | `test_delivery_falls_back_to_code_when_the_model_never_complies`, `test_delivery_reports_a_pause_instead_of_composing` | `delivery.composed_by_code` | `results/red_team.json` (`never_complies`) | `red_team.composed`, `exhaustive.composed` |
| Model calls per run are bounded (Strands' after-model retry loop is not) | `BudgetedModel` inside `build_agent` | `test_delivery_falls_back_to_code_when_the_model_never_complies`, `test_budget_cap_is_enforced` | | | |
| Every retry is sendable to Bedrock at the first attempt (roles alternate; the neutral turn after a tool result is placed ahead of time) | `sendable_turns` in the cap wrapper, request-local | `test_adjacent_user_text_turns_fold_into_one_and_nothing_else_changes`, `test_after_two_guides_the_model_never_sees_two_adjacent_user_turns`, `test_every_retry_is_accepted_at_the_first_attempt`, `test_without_the_passes_the_second_guide_is_unsendable`, `test_fuzz_every_conversation_strands_can_leave_is_sendable_after_the_passes` | | `results/bedrock_wire.json` | `wire.every_retry_sendable`, `wire.sdk_separator_costs_one`, `wire.no_passes_falls_back_to_code` |
| The request Bedrock receives is known before the first live call: the system prompt, `draft_plan` and the `Plan` tool, the rider's prompt, the tool result with the approved sentences | `BedrockModel.format_request` (the real adapter) over a stand-in client | `test_the_request_bedrock_receives_carries_the_system_prompt_both_tools_and_the_prompt`, `test_the_second_request_carries_the_tool_result_with_the_approved_sentences` | | `docs/evidence/bedrock-request.json` | `wire.retries_plan_is_the_models` |
| Every AWS call the apply paths make is well formed before Sunday: parameter names, required members, enums and lengths checked against the service model botocore ships | `check_calls` in `le_dispatch/api_shapes.py`, run by the dry runs and `make preflight` | `test_the_policy_plan_is_well_formed_for_a_new_and_for_an_existing_gateway`, `test_the_evaluation_calls_are_well_formed_and_the_session_id_is_long_enough`, `test_update_gateway_without_the_required_members_is_rejected_which_is_what_the_old_plan_sent`, `test_the_custom_evaluator_reads_the_ground_truth_in_the_api_shape` | | | |
| A model error, a response cut off by the token limit or a stream silent past the read timeout is one call, not a loop, and the rider still gets the plan; a throttle is retried by the SDK | `deliver` over the SDK's `ClientError`, `MaxTokensReachedException` and `ModelThrottledException` paths and botocore's `ReadTimeoutError` | `test_a_permission_error_costs_one_call_and_the_rider_still_gets_the_plan`, `test_a_truncated_response_costs_one_call_and_the_rider_still_gets_the_plan`, `test_a_throttle_is_retried_by_the_sdk_and_counts_against_the_cap`, `test_all_scenarios_and_the_script_write_stable_files` | `delivery.composed_by_code` | `results/bedrock_wire.json` | `wire.access_denied_costs_one_call`, `wire.truncated_costs_one_call`, `wire.hung_costs_one_call`, `wire.throttle_retried` |
| The gates fail closed | `try/except` returning `Guide` in both steering methods | `test_gates_fail_closed_on_internal_error` | `steering.guide_after_model` | | |
| A gate the SDK did not invoke cannot leak: the delivery layer runs the same checks once more before anything reaches the rider | the final check in `deliver` and in `DecisionRun._finish` (honours the ablation switches) | `test_delivery_rechecks_the_plan_even_if_the_gate_never_fired`, `test_decision_run_rechecks_the_plan_even_if_the_gate_never_fired` | `delivery.final_check_failed` | | |
| Every case in the KB, every attack, nothing reaches the rider | `run_red_team_exhaustive` over the full stack | `test_exhaustive_red_team_on_a_slice_and_its_arithmetic` (slice) and `make red-team-exhaustive` (all) | all of the above | `results/red_team_exhaustive.json` | `exhaustive.runs`, `exhaustive.delivered` |
| The gates' feedback is actionable: a wrong-from-the-start model recovers on every case, and the hook's cancel message names the affected station and elevator | the reason strings in `KBHook` (with the decision), `steer_before_tool`, `check_plan` | `test_each_persona_recovers_from_feedback`, `test_convergence_over_a_slice_matches_the_full_run_shape`, `test_a_model_that_ignores_feedback_hits_the_cap`, `test_the_hook_names_the_affected_station_and_elevator_and_the_learner_recovers_from_it_alone` | | `results/adaptive_convergence.json` | `convergence.runs`, `convergence.converged`, `convergence.max_calls` |
| The feedback is readable in both tool-result formats the adapter sends (JSON with a status flag for Anthropic, text without one for Nova): every persona converges through the real adapter | `PersonaRuntime` and `run_wire_convergence` in `le_dispatch/bedrock_wire.py` | `test_every_persona_converges_through_the_adapter_in_both_tool_result_formats` | | `results/wire_convergence.json` | `wire_convergence.runs`, `wire_convergence.converged`, `wire_convergence.max_calls`, `wire_convergence.rejected` |
| Prose checks never miss a wrong minute or a foreign code, never flag a right one | `prose_findings` | `test_prose_findings_fuzz_never_misses_a_wrong_minute_or_foreign_code` | | | |
| The counters would see a leak | the no_steering ablation | `test_counters_see_leaks_when_steering_is_off`, `test_no_steering_ablation_lets_wrong_option_through` | | (never cited) | |
| The rider is asked once, even when the gates retry the call the answer resumed (a second interrupt would be raised for the new tool use id) | `DecisionInterruptHandler.is_interrupted_call`: the Interrupt is returned again only for the interrupted call; later calls in the resumed pass proceed or hold on the answer | `test_a_resumed_call_the_gates_retry_does_not_ask_the_rider_again`, `test_the_human_moment_on_the_wire_asks_once_and_every_request_is_accepted` | `interrupt.raised`, `interrupt.resumed` | `results/bedrock_wire.json` | `wire.human_yes_asked_once`, `wire.human_no_asked_once` |
| The rider is asked only after dark or at last train | `DecisionInterruptHandler.steer_before_tool` (Interrupt) | `test_run_pauses_writes_card_and_resumes_on_yes`, `test_daytime_case_never_interrupts` | `interrupt.raised` | `results/quiet.json` | `quiet.interruptions`, `quiet.decisions` |
| The decision card is complete and its numbers are code's | `DecisionCard.from_decision` | `test_run_pauses_writes_card_and_resumes_on_yes` | `interrupt.raised` | `docs/evidence/*.json` | |
| A decision, what was sent, and an open question outlive the runtime session (never asked twice, never sent twice, across sessions) | `DecisionMemory(store=AgentCoreDecisionStore)`, `SentLog(store=AgentCoreSentStore)` and `Inbox(store=AgentCoreCardStore)`: write-through on every answer, delivery and card, read-through for an unknown case, a cleared event when the outage ends; `FakeMemoryClient` validates every call against the service model | `test_a_decision_survives_a_session_that_never_took_it`, `test_the_sent_log_durable_copy_stops_a_fresh_session_from_sending_twice`, `test_an_open_question_survives_a_recycled_runtime_session_without_being_asked_again`, `test_the_runtime_takes_a_store_factory_and_reads_the_memory_id_from_the_environment`, `test_every_call_the_store_makes_is_well_formed` | | | |
| A pending question is not asked again on the next poll | `DecisionRun.start` reads the inbox first | `test_pending_case_is_not_asked_again_on_the_next_poll` | | `results/quiet.json` | |
| The inbox survives the poller and the app writing at once | `Inbox._locked` (an advisory lock per read-modify-write) and atomic saves (`os.replace`); the decision mirror saves atomically too | `test_inbox_writes_are_atomic_and_serialized_across_threads` | | | |
| One outage, one message: a plan sent for a case is not sent again on the next poll and no model runs for it; a new outage of the same elevator is a new message | `SentLog` in the runtime entrypoint (`already_sent`), cleared when the elevator is back | `test_one_outage_one_message` | | | |
| A question does not outlive its outage: an open card is withdrawn when the elevator is back, never re-asked, and a late answer is refused | `Inbox.withdraw_stale`, called on every poll by the runtime entrypoint | `test_open_question_is_withdrawn_when_the_elevator_comes_back`, `test_entrypoint_withdraws_an_open_question_when_the_elevator_is_back` | | | |
| One retry layer on the live path: botocore's own retries are off (one request per attempt) and Strands' strategy is bounded (four attempts, waits of 2, 4 and 8 seconds), so a throttled model call costs at most four requests and fourteen seconds, a hung stream sixty, and every retry is one the trace shows and the budget counts | `le_dispatch/live.py` (`client_config`, `retry_strategy`, `model`, `worst_case`), used by the runtime's `bedrock_model_factory`, `make demo-live`, `make eval-live` and the wire's stand-ins | `test_botocores_own_retry_layer_is_off_on_the_live_client`, `test_strands_layer_is_bounded_and_the_arithmetic_is_pinned`, `test_the_bounded_layer_through_the_adapter_three_throttles_then_the_plan_and_four_then_code`, `test_every_live_construction_is_on_the_live_configuration` | | `results/bedrock_wire.json` | `wire.throttle_retried` |
| The hosted contract holds over the whole dataset: every case through the runtime entrypoint with five riders (daytime; daytime with a note that only gives orders; daytime with a note that names a real constraint and gives orders too; after dark, never answers; after dark, says no), the plan the model's with the policy engine's option and minutes, one message per outage, a note that gives orders changing nothing and a note with a real constraint taking one option away and nothing else, one question per night, the morning poll closing it, a no held | `le_dispatch/runtime_sweep.py` over `handle()`, the learner through the real adapter, the resilient Memory stand-in | `test_the_sweep_holds_on_a_slice_and_says_what_it_counted`, `test_the_script_writes_the_results_file_and_exits_zero_on_a_complete_slice` | `interrupt.raised`, `interrupt.resumed`, `interrupt.superseded` | `results/runtime_sweep.json` | `runtime_sweep.cases`, `runtime_sweep.agreement`, `runtime_sweep.minutes`, `runtime_sweep.one_message`, `runtime_sweep.asked_once`, `runtime_sweep.superseded`, `runtime_sweep.held`, `runtime_sweep.note_orders`, `runtime_sweep.note_removes`, `runtime_sweep.composed_by_code`, `runtime_sweep.failures` |
| The durable copy is best effort and the delivery is not: a Memory call that fails (a throttle, an outage, a permission error) degrades the copy and the invocation delivers from the session's own; a write that failed is replayed before the next call with its original client token, so the copy catches up without duplicates | `ResilientMemoryClient` (one per container, `decision_store_factory`), `create_event_params` (a client token derived from the write), the `memory` field on every runtime response | `test_a_memory_outage_never_fails_an_invocation_and_the_backlog_replays`, `test_a_replay_or_a_retry_of_the_same_write_is_stored_once`, `test_the_resilient_client_keeps_order_and_stops_at_the_first_failure_on_replay` | `memory.degraded`, `memory.replayed` | | |
| A question never blocks: an unanswered night question met by the morning poll (the outage still on, the rule that asked no longer holding) is closed as superseded, the paused run released, and the plan sent without a second question | `DecisionRun.start` (`needs_human` on the current decision, `Inbox.withdraw` with `SUPERSEDED_REASON`, `is_paused`) | `test_the_morning_after_sends_the_plan_and_closes_the_night_question`, `test_the_morning_after_in_a_recycled_container` | `interrupt.superseded` | | |
| An answer never fails: a double tap, a changed mind after a decline, and an answer to a question this container never asked each return a plain state (what was delivered, or the plan drafted under the answer), never an error | the entrypoint answers from the sent log first; `DecisionRun.resume` remembers the answer and runs afresh when no paused run exists | `test_an_answer_never_fails`, `test_an_open_question_survives_a_recycled_runtime_session_without_being_asked_again`, `test_pause_and_resume_across_processes_with_file_session_manager` | `interrupt.resumed` | | |
| Every decision states what it cost: model calls, tokens, event-loop cycles, from Strands' own metrics | `run_usage` on the delivery outcome, the `cost` block of the packet, `tokens_used` per live-eval entry | `test_every_outcome_and_packet_states_what_the_decision_cost` | | `docs/evidence/*.json` (`cost`), `results/policy_agreement.json` (`tokens_used`) | |
| What the agent keeps about a rider has a retention rule | `Inbox.purge` and `scripts/retention.py` (dry run unless `--yes`; open cards never purged) | `test_retention_drops_only_closed_cards_older_than_the_rule` | | | |
| What the rider hears is in plain words: the station's name when the KB has one, BART's option in words, the flags as sentences | `card_question`, `approved_messages`, `KBSet.station_names` through `PolicyDecision.spoken_station` | `test_card_question_is_in_plain_words`, `test_station_names_reach_every_sentence_and_the_card_when_the_kb_has_them`, `test_every_sentence_and_card_over_every_case_reads_aloud_cleanly` | | | |
| The sentence never names a BART option other than the plan's | `prose_findings` (label words, "transit" excepted for AC Transit) | `test_prose_names_the_wrong_option` | `steering.guide_after_model` | | |
| The same case never interrupts twice | `DecisionMemory` in `agent.state` plus the JSON mirror | `test_same_case_does_not_interrupt_again`, `test_pause_and_resume_across_processes_with_file_session_manager` | `interrupt.resumed` | `results/quiet.json` | `quiet.background` |
| The rider's own words reach the plan only as feasibility: the model reads the note into a fixed vocabulary with the rider's words as the reason, code checks the quotes, marks the ruled-out options infeasible, keeps BART's order and the policy engine's minutes, sets the note aside when nothing is left, and the card says what was asked | `le_dispatch/note.py` (`RiderNote`, `check_quotes`, `apply_constraints`, `constrained`, `read_note`), the runtime's `note` payload field | `test_a_constraint_only_takes_options_away_and_barts_order_stands`, `test_the_model_may_only_point_at_the_riders_words`, `test_the_runtime_reads_the_note_and_the_learner_stand_in_reads_by_its_words` | | | |
| The conversation belongs to its outage: the first quiet poll after the outage deletes the trip's Strands session (messages, tool results, a paused interrupt), so the next outage starts clean and a rider's history never accumulates in the session store | `reset_session` in the runtime entrypoint through the session manager's `delete_session` (file or S3) | `test_the_conversation_belongs_to_its_outage` | | | |
| A decision belongs to its outage: when the elevator is back the answer is forgotten in the mirror, the session's copy and the durable copy (a cleared event, a tombstone against the session's stale copy), so the next outage of the same elevator asks afresh rather than being held or sent on an old answer | `DecisionMemory.clear_stale` on every poll, `attach` dropping what the tombstones or the durable copy say is gone, `AgentCoreDecisionStore.clear` | `test_a_decision_belongs_to_its_outage`, `test_the_memory_forgets_a_decision_when_its_outage_ends_in_every_copy` | `decision.cleared` | `results/runtime_sweep.json` | `runtime_sweep.asked_again` |
| A decline keeps BART's plan on file, marked hold, never re-sent | `DecisionInterruptHandler.steer_after_model` | `test_decline_cancels_tool_and_holds_plan`, `test_model_cannot_hold_without_the_rider` | `steering.guide_after_model` | | |
| The pause survives a process boundary | Strands session manager via `build_decision_run(session_manager=...)` | `test_pause_and_resume_across_processes_with_file_session_manager` | `interrupt.resumed` | | |
| Most work happens in silence | the replay in `replay_week` | `test_replay_week_counts_and_opening_line` | | `results/quiet.json` | `quiet.days`, `quiet.riders` |
| Tool calls through the gateway carry only KB stations, KB elevators, BART labels | Cedar in `infra/agentcore/policy/` (default deny, forbid wins) | `test_local_cedar_evaluation_denies_fake_station_and_option`, `test_cedar_contains_every_station_and_label` | | `infra/agentcore/policy/manifest.json` | |
| The Cedar would pass AgentCore's creation-time validation | `schema_for` plus `validate_locally` | `test_generated_policies_validate_against_the_schema` | | `infra/agentcore/policy/manifest.json` (`local_validation`) | |
| The AgentCore evaluator reads what Strands really emits | `evaluators/option_equality.py` on `gen_ai.tool.message` events | `test_evaluator_reads_real_strands_spans`, `test_custom_evaluator_on_three_mock_traces` | | `results/agentcore_eval_local.json` | `agentcore.local_pass`, `agentcore.local_fail` |
| The policy engine agrees with the frozen cases it embodies (the laptop's real engine; the case-backed policy against itself here) | `le_dispatch/policy_check.py`, `make policy-check POLICY=...` | `test_the_case_backed_policy_agrees_with_itself_on_every_case`, `test_a_drifting_engine_is_listed_case_by_case` | | `results/policy_engine_agreement.json` | `policy_check.cases`, `policy_check.disagree` |
| The ground-truth dataset is code's decision per case | `export_dataset` | `test_dataset_export_matches_documented_schema` | | `results/agentcore_dataset.json` | `agentcore.dataset`, `agentcore.stations`, `agentcore.elevators` |
| No AWS call without credentials and an explicit yes | `credentials_present` plus `--yes` in every apply path | `test_apply_refuses_without_credentials_or_yes`, `test_dry_run_plan_prints_stats_and_calls_without_network` | | | |
| Tests cannot reach the network or a key | `tests/conftest.py` guards | every test (autouse fixtures) | | | |
| Live rows are capped and never overwrite local rows | `BudgetedModel`, `merge_entries`, `write_scores` | `test_budget_cap_is_enforced`, `test_results_writer_never_overwrites_local_entries`, `test_runner_writes_four_entries_with_model_ids` | | `results/policy_agreement.json` | |
| The gateway's Cedar and the in-process hook enforce the same rule | Cedar generated from the same KB the hook reads | `test_cedar_and_python_hook_agree_on_random_inputs` | | | |
| A bad payload runs nothing: state `invalid` with a reason, no model, no file, no state change; a hundred random payloads never raise | `trip_from` validation in the entrypoint | `test_invalid_payloads_never_run_anything` | | | |
| The evaluation dataset runs against the deployed agent unchanged: each turn's input is the payload as JSON, and `{"prompt": ...}` invocations (the runner's and `agentcore invoke`'s shape) reach the same contract | `payload_from_prompt` in the entrypoint, `scenario_input` in the exporter | `test_prompt_shaped_invocations_reach_the_same_contract` | | `evals/agentcore/dataset.json` | |
| The runtime payload contract works offline (quiet, sent, pending, resume), the real AgentCore app serves it, and the toolkit-facing module exposes it at module level | `infra/agentcore/runtime/entrypoint.py` (`make_runtime` on `BedrockAgentCoreApp`), `infra/agentcore/runtime/app.py` | `test_entrypoint_quiet_sent_pending_and_resume`, `test_agentcore_app_serves_ping_and_invocations_in_process`, `test_the_toolkit_facing_app_module_exposes_a_module_level_app` | | | |
| The harness is agency-agnostic: another KB and order, zero code changes, the hosted contract included | the KB and cases interfaces plus the policy callable; `run_sweep` takes the agency's own far stations | `test_policy_ranks_in_the_agencys_own_order`, `test_gates_and_messages_work_unchanged`, `test_exhaustive_red_team_on_the_other_agency`, `test_cedar_and_dataset_generate_from_the_other_kb`, `test_convergence_on_the_other_agency`, `test_the_second_agency_runs_through_the_same_entrypoint` | | `results/runtime_sweep_synthetic.json` | `sweep_synthetic.cases`, `sweep_synthetic.agreement`, `sweep_synthetic.note_orders`, `sweep_synthetic.failures` |
| Every number in the docs comes from results and is labelled | `scripts/check_docs.py`, `scripts/verify_claims.py` | `make verify` | | all of `results/` | 84 claims checked |
| A fixture run never overwrites a results file a real run wrote: the laptop's numbers survive a target typed without the exports or the archive, and the main repo's CI, which has no archive, leaves the archive's files as committed so its determinism check stays clean; for the export-sourced files the placed workflow reads `kb/export/` once it is committed, and `POLICY` in the main Makefile runs the real engine there | `write_results_json`, `keep_the_real_file` and `written_by_a_real_run` in `le_dispatch/interfaces.py`, the one writer every results file goes through; the exports step of `.github/workflows/verify.yml` | `test_a_fixture_run_never_overwrites_a_results_file_a_real_run_wrote`, `test_a_fixture_run_never_overwrites_the_real_archives_numbers`, `test_a_synthetic_replay_never_overwrites_the_archives_week` | | all of `results/` | |
| Which gate protects what: the after-model gate is load-bearing, the approved-sentence check covers the prose checks, the before-tool gates never change what reaches the rider | `build_agent(disabled=...)` switches in `gates.py`, the study in `le_dispatch/ablation.py` | `test_after_model_gate_is_load_bearing_and_before_tool_gates_are_not`, `test_prose_checks_are_subsumed_by_the_approved_sentence_check` | | `results/gate_ablation.json` | `ablation.full_stack_leaked_runs`, `ablation.without_after_model_gate_leaks`, `ablation.without_approval_leaks_injection`, `ablation.without_prose_still_zero`, `ablation.every_plan_delivered` |
| Every plan-gate reason names the right value, so the plan gate alone leads a model to the plan | `check_plan` reasons | `test_plan_gate_reasons_alone_lead_the_stand_in_model_to_the_plan` | `steering.guide_after_model` | `results/gate_ablation.json` | `ablation.plan_gate_alone_converges` |
| An unaffected trip never runs the model | `deliver` returns before the agent runs; the runtime reports quiet | `test_unaffected_trip_is_refused`, `test_entrypoint_quiet_sent_pending_and_resume` | `delivery.quiet` | `results/quiet.json` (`quiet_trips`) | |
| Every path and Strands symbol the docs name is real | `check_paths_and_symbols` in `scripts/check_docs.py` (imports each symbol in the "Strands surface used" table) | `make check-docs` | | | |
| The evidence site a judge reads works with a screen reader and a keyboard | `scroll_regions` in `scripts/site.py` (every code block and wide table focusable), the site's contrast; `scripts/site_a11y.py` runs axe-core over every page in Chromium and exits 1 on a violation | `test_a_few_pages_of_the_built_site_pass_axe`, `test_the_committed_audit_covers_the_site_and_names_every_rule_that_fired` | | `results/site_a11y.json` | `site.a11y.violations`, `site.a11y.pages` |
| The same gates hold on a live model | `scripts/demo_live.py` builds the agent through `build_agent` and delivers through `deliver` | laptop run, `make demo-live DEMO_LIVE_ARGS="--yes"` | all of the above | `docs/evidence/live-<model>.md` (pending) | |

Evidence packets for one run each (JSON and markdown with a sequence
diagram, regenerated by `make evidence`): `docs/evidence/DELN-E1-daytime`
(every gate fires), `DELN-E1-after-dark-yes` (the human moment, resumed),
`DELN-E1-after-dark-no` (declined, plan held on file), `DELN-E1-note` (the
rider's own words read into the vocabulary and applied as feasibility: the
ramp route infeasible, BART's next option with the policy engine's
minutes). All from the fixture KB; the laptop regenerates them on the
real KB and adds the live-model packet from `make demo-live`.

The one Strands limitation this package works around, the unbounded
after-model retry loop, is documented with an offline reproduction in
`docs/UPSTREAM-NOTE.md`.
