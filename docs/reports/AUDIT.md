# Audit: Last Elevator, Sunday September 13, 2026, 23:50 to Monday 00:40 PDT

Audited from the dispatch worktree (`last-elevator-dispatch` at `f98396a`, 354 commits), which is the
standalone package, not the integrated `agents-for-humans` repository. Everything the laptop owns (the
rider app, the poller and its archive, the frozen `kb/`, the main README, `links.json`, Blocks B to E)
is marked "not audited here" with the reason; nothing about it is assumed. Every number below was
regenerated or read from `results/` during this audit, never copied from a README.

## A. Progress

| Area | Status | Proof | Missing if partial |
|---|---|---|---|
| Blocks B, C, D, E (poller, parser, policy engine, rider app, archive, labelers) | not audited here | they live in `agents-for-humans`; this worktree carries their documented interfaces and fixtures instead | an audit of those needs the laptop repo |
| Block F, items F1 to F101 | done, 99 rows in the package README's item table, 102 rows in `docs/SUBMISSION-CHECKLIST.md`, 92 marked done | `git log --oneline` 354, `git status` clean, 49 bundles verified in `bundles/INDEX.md` | the 10 non-done rows are all owner or laptop steps, listed below |
| Block G (integration) | not started | no integrated repo reachable from here | the laptop runs it; the corrected order is in the delivery |
| Non-done checklist rows | 10 | `docs/SUBMISSION-CHECKLIST.md` | public repo URL, Built With tags, Builder ID, track selection, live URL, runtime ARN, the devpost TODOs, the README merge, F0 reconciliation |
| Tags | none in this worktree | `git tag` empty | `v1.0-submission` is tagged in the main repo at merge |

## B. Substance, regenerated

| Metric | Value | How it was obtained |
|---|---|---|
| Tests collected | 239 | `pytest --collect-only -q`, summed per-file counts |
| Claims verified | 84 of 84 | `python3 scripts/verify_claims.py` tail |
| Line coverage | 95.3 percent of 3699 statements | `results/coverage.json`, regenerated by the coverage target in this session |
| Python lines | 19,610 total: package 7,481, scripts 5,879, tests 5,798, runtime 452 | `find ... -name "*.py"` excluding build, virtualenvs, caches, then `wc -l` |
| Tracked files | 229 | `git ls-files` |
| Documentation words | 51,269 across `docs/` | `wc -w` |
| Knowledge base (fixture shape; the real one is the laptop's frozen `kb/`) | 50 stations, 97 elevators, 194 cases, labels 86 transit / 55 alternate elevator / 53 backtracking, five option labels | `fixtures/kb.json`, `fixtures/cases.json` read directly |
| Red team | 140 runs, 7 attack kinds, reached the rider: 0 stations, 0 options, 0 minutes, 0 unapproved sentences, 140 plans delivered, 20 composed by code | `results/red_team.json`, `claimable: false` |
| Red team, exhaustive | 2716 runs, 194 composed by code, same zeros | `results/red_team_exhaustive.json` |
| Gate ablation | with every gate 0 leaked; without the after-model gate 120 of 140 leaked; nine configurations | `results/gate_ablation.json` |
| Convergence | 1746 of 1746 runs reached the policy engine's plan, at most 5 model calls | `results/adaptive_convergence.json` |
| Hosted contract sweep | 194 cases, 2328 invocations, 970 model runs, 0 composed by code | `results/runtime_sweep.json` |
| Evidence site accessibility | 35 pages through axe-core, 0 violations, 0 rules failed | `results/site_a11y.json` |
| Quiet metric | 7 days, 3 riders, 2 interruptions, 2 decisions, week and riders both synthetic | `results/quiet.json` |
| Impact from the feed | 5 outages, 5 stations, 52.8 elevator-hours, archive `fixture` | `results/outage_week.json` |
| Policy agreement rows | 2 rows, both provider `mock`, model `mock-echo`, 194 cases, marked not claimable | `results/policy_agreement.json`: no live row exists yet |
| Archive | not present here | no `data/archive/manifest.json` in this worktree; it is the laptop's |
| Screenshots | 10 images plus a README | `docs/screenshots/` listing |
| Posts | 3 drafts and 3 publish-ready copies, each title carrying the hackathon name | `docs/posts/` |
| Video | script, storyboard and verified command list present; no video file, no URL | `docs/video-script.md`, `docs/video/` |
| Deployment | none | no ARN and no live URL anywhere in the worktree |
| Real riders | none claimed | no rider count in any results file; every rider in the package is synthetic |
| LICENSE | MIT at the repository root | first line of `LICENSE` |
| Placeholders | 0 double-brace placeholders; 13 TODO markers in the sections the owner merges, 11 in the submission text, 2 in the package README (the badge owner and repository), 1 per publish-ready post (its two links) | grep counts |
| Strands controls present in code | hook on the before-tool event with tool cancellation, steering before the tool, steering after the model, structured output, file and S3 session managers, interrupts, a bounded retry strategy, the real Bedrock adapter, the AgentCore runtime app | grep over the package and the runtime; twelve import lines, all resolving |
| One run, observed live in this audit | hook cancels 1, guide before tool 1, guide after model 1, plan delivered with the policy engine's option and minutes, sentence composed by code | the brief demo and the trace demo, run just now |
| Secret scan | clean over 229 tracked files; no environment file or credential ever tracked | the scan script, and a history search over every tracked path |

## C. Rubric

| Criterion | Score now | Evidence | What a 10 looks like here | Gap | Actions, best value first | Projected Monday noon |
|---|---|---|---|---|---|---|
| Stage One | pass | background poller and the quiet metric (7 days, 2 interruptions) for the theme; twelve Strands imports resolving, three controls observed firing in one run | same | none | none | pass |
| Technical Implementation | 8 | every control plus five beyond the build session: after-model steering, interrupts, a scripted offline model, a red team through the full agent, span events; AgentCore runtime, policy and evaluations scaffolded and dry-run | all of that plus a live URL, a printed runtime identifier, and live evaluation rows on two models | no deployment, no live rows: `results/policy_agreement.json` is two mock rows | 1. the runtime deploy, identifier into the README (laptop, 1.5 h). 2. the two-model live evaluation at 40 cases (laptop, 0.5 h) | 10 |
| Design | 6 | decision cards, the quiet report, the rider note path, the evidence site at zero violations over 35 pages | rider onboards, gets a plan, answers a card, reads a weekly report, all on video, zero violations | the rider app is the laptop's and not auditable here; no video exists yet | 3. cut the video with the app on screen (owner, 1.5 h) | 8 |
| Potential Impact | 7 | a named person in the first sentence, the settlement and the availability goal cited with sources, the feed summarised by the same code the laptop runs on the real archive | the same plus the real archive week and at least one real rider watched | the impact numbers come from the fixture archive; no real rider | 4. the archive replay for the report and the impact paragraph (laptop, 1 h) | 8 |
| Creativity and Originality | 8 | policy as steering, measured by removing one gate at a time; a red team through the full agent; a frozen labelled knowledge base; a written contribution back to the SDK with a test that flips when it is fixed | the same with the ablation on the real engine and the upstream note posted | both are fixture-run and unposted | 5. post the upstream note and link it (owner, 0.25 h) | 9 |
| Presentation | 3 | a script whose voice lines are all under twelve words, a storyboard, a verified command per on-screen moment, 10 screenshots, an animated first shot | under four minutes, the person and the number in the first thirty seconds, the gates visible, captions, the description reading as guarantees | there is no video | 3, again: this is the single largest lever in the whole audit | 8 |
| Bonus posts | 0 of 0.6 | three publish-ready posts, each title carrying the hackathon name, 728, 872 and 736 words | three published before the deadline | none published; no URLs | 6. publish all three (owner, 0.75 h) | 0.6 |
| Hidden requirements | mostly met | MIT license at the root; a cold-start transcript ending green; the disclosure paragraph; synthetic riders only; the architecture document naming interface, loop, tools, AWS services and output | every box ticked on the form | public repository, Built With tags, Builder ID, track, video visibility all pending with the owner | 7. the form pass (owner, 0.5 h) | met |

Total now, five criteria equally weighted: 6.4. Projected if actions 1 to 7 land by noon: 8.6 plus the bonus.

**Technical Implementation.** Proven: the three gates fire in one observed run, with the cancel reason,
the before-tool guide and the after-model rejection all naming the right value; 84 machine-checked claims;
2716 adversarial runs with four counters at zero; the live path exercised through the real Bedrock adapter
with a stand-in client, so the exact request is on file before the first live call. Claimed but not proven:
anything hosted. A judge who reads only the first page sees a deterministic agent with numbers, and no
live thing to click. The single change that moves this most is the deployment plus one live evaluation
row, because the calibration puts the ceiling there and nowhere else.

**Design.** Proven here: the decision card, the quiet report, the note path, and an evidence site audited
page by page at zero violations. Not provable from this worktree: the rider app, which is the laptop's.
A judge watching the first minute currently sees nothing at all, because there is no video. The single
change is the video with the app on screen; the product's completeness is not in question, its visibility
is.

**Potential Impact.** Proven: a specific person in the first sentence, a settlement cited as context with
an explicit statement that this product satisfies no legal requirement, the availability goal and the
population figure sourced, and an impact summary computed by the same code that will run on the real
archive. Claimed but not proven: that those impact numbers describe a real week; they describe the
fixture archive and say so. A judge reading the first page gets a credible, sourced problem. The single
change is the archive replay, which turns five fixture outages into a real week.

**Creativity and Originality.** Proven, and the strongest column: policy as steering rather than as a
prompt, with an ablation that says which gate carries the guarantee (remove the after-model gate and 120
of 140 runs leak; remove the before-tool gates and none do), a red team that runs through the full agent
rather than against a string, and a written contribution back to the SDK. Not proven: the ablation on the
real engine. A judge who reads the first page sees an argument no other entry is likely to make. The
single change is posting the upstream note, which converts a document into a visible contribution.

**Presentation.** Proven: every asset a video needs except the video. Not proven: that the project works,
to anyone who will not run a command. A judge who watches the first minute of nothing scores this 1. The
single change is cutting the video, and it is worth more than everything else in this audit combined,
because one fifth of the total currently sits at 3.

## D. Field

Gathered from the public web during this audit.

- The hackathon's own project gallery is not published yet ("the hackathon managers haven't published this
  gallery yet"), so the field cannot be surveyed and no entrant can compare themselves to it. The
  submission page has to carry the whole argument on its own.
- Organizer updates: the newest is a deadline reminder two days ago, then the recorded build session four
  days ago. Nothing newer than September 9 changes the requirements; the standing ones are naming Strands
  explicitly, a public repository with all source, a description covering problem, solution and audience,
  and a video.
- The organizers' own hackathon guidance post argues for genuinely avoided, judgement-heavy tasks and
  points at tools, composition and autonomous reasoning. It does not mention hooks, steering, determinism
  or guardrails. Read together with the brief's "prioritise making the agent more deterministic", our
  framing should lead with the rider's avoided task and the background work, then present determinism as
  how it is made trustworthy, rather than opening on the gate architecture.
- Last cycle's first place (a waste-reporting agent) carried a live site, a documentation site, a mobile
  app, an explicit runtime identifier, ten gallery images, and stated counts of reports, users and agent
  interactions. Its first sentence is autobiographical and local. Best Strands went to a project with a
  non-obvious embodiment idea and a security story, one diagram, and no deployment identifier: a reminder
  that a single strong idea can win a named prize without a full product.
- A visible competitor in this hackathon, a re:Invent route planner built on the SDK, already has a
  working public demo behind a content delivery URL, a multi-agent design, tests and documentation, and
  it uses the same background framing ("only pings you when a real decision is needed"). It shows no
  hooks, steering, interrupts or evaluation work. Rated high threat on completeness, low on determinism.
- Five sentences on where we sit. On Technical Implementation we are ahead of everything visible in the
  field on depth of SDK use and behind the completed entries on deployment. On Design we have more product
  surface than most and less of it visible, because the video is unmade. On Potential Impact our framing
  is stronger and better sourced than the winners we sampled, and our numbers are fixtures where theirs
  were real. On Creativity we are the only entry seen that measures which control carries its guarantee,
  and that is the argument to lead with. On Presentation we are currently last among anyone who has a
  video at all. The one thing a top-six project has that we do not is a URL a judge can click and a
  number of real people who used it.

## E. Integrity

- Every number in the sections the owner merges and in the submission text traces to a results file or a
  cited source. Two exceptions, both honest: the screenshot numbers (00 to 09) are file names, and "41
  claims at last report" refers to the main repository's own claims table, which cannot be verified from
  this worktree and should be re-counted on the laptop after the merge.
- Placeholders, by design and all labelled: 13 TODO markers in the sections the owner merges, 11 in the
  submission text (the Links section keeps a labelled line per field), 2 in the package README (the badge's
  owner and repository), and one per publish-ready post (its two links). No double-brace placeholder
  remains anywhere.
- No overclaim found. The settlement is cited as context and the text states plainly that the product
  satisfies no legal requirement. The mock evaluation rows are labelled plumbing proof and are never
  presented as an ablation. No "real users" claim exists anywhere; every rider is synthetic and says so.
  Every Strands control named in the documents resolves to an import and a file.
- No rule violation found: the knowledge base is untouched here (the frozen one is the laptop's), the
  secret scan is clean over 229 tracked files, no environment file or credential appears in the history,
  and no rider data of any kind is in the repository.
- One residual risk worth naming: the claims badge on the first screen renders broken until the owner
  replaces the two repository placeholders in its URL.

## F. Verdict

1. Submit-ready: yes for the code and the words, no for the submission as a whole, because there is no
   video and nothing deployed.
2. Blocking, in order: the video; the public repository with the license visible; the form's own fields.
3. Not blocking but worth the most points: the deployment identifier, one live evaluation row, the
   archive replay, the three posts.
4. Action one, the video, owner, about ninety minutes: everything it needs exists, including a verified
   command and a freeze line per shot. It moves one fifth of the total from 3 to about 8.
5. Action two, the laptop's live runs and deploy, about two hours: it moves Technical Implementation to
   the ceiling and lifts Impact with the archive replay.
6. Action three, publish the three posts and the upstream note, owner, about one hour: the bonus in full
   and the contribution made visible.
7. Projected total if those land by noon: about 8.6 of 10 plus the bonus, against 6.4 today.
8. Cut without regret: the gateway smoke test, the AgentCore evaluation run, the public dataset export,
   the memory resource. Each is scaffolded, dry-run and labelled pending; no judge will look for them.
9. The one thing the field's winners have that we do not is a link a judge can click and a count of real
   people who used it; the first is two hours away, the second is not reachable by noon and should not be
   claimed.
10. Nothing in this audit requires a code change. The remaining score is in the video, the deploy, and
    the form.
